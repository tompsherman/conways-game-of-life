import React, { useContext, useEffect, useState} from 'react'
import { GridContext } from "./context/GridContext"

const GridSquare = (props) => {
    
    const { row, column, living } = props

    return <></>
}

export default GridSquare
